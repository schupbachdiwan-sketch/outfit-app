# 开发日志 — 2026-06-10

## 今日完成

- [x] **修复 ApiClient 超时 Bug**（`lib/core/network/api_client.dart`）：
  - `tryOnBytes()` 超时从 60s → 200s
  - **根因**：AI 试衣实际需要 ~90s，60s 超时导致 Flutter 端在结果返回前主动断开连接

- [x] **身体照片 AI 模特化预处理**（全新功能）：
  - 新增 `/api/generate-model` 端点 — 代理服务器 v0.4.0
  - 流水线：rembg 去背景 → 合成摄影棚背景 → Wan2.7 姿态优化
  - 实测：23.6s，输出 1773×2364 高清模特图 ✅
  - `/api/try-on` 新增 `preprocess_body` 参数，一步完成预处理+试衣

- [x] **Flutter 端全链路接入**：
  - `ApiConfig` 新增 `generateModelPath`
  - `ApiClient` 新增 `generateModel()`；`tryOnBytes()` 支持 `preprocessBody`
  - `TryOnService` 新增 `generateModel()`；`tryOnSingle()` 支持 `preprocessBody`
  - 试衣间步骤1：拍照后自动调用 AI 模特化，弹窗显示进度
  - 试衣间步骤4：自动检测身体是否已预处理，未处理则先预处理再试衣
  - `flutter analyze` 零 error 零 warning ✅

- [x] **测试脚本**：
  - `tools/test_tryon_pipeline.py` — 端到端测试试衣流水线
  - `tools/test_generate_model.py` — 端到端测试模特化流水线

## 架构变更

```
旧流程（v0.3.0）:
  用户照片 → aitryon-plus 直接套衣服 → 效果差（背景乱、姿势差）

新流程（v0.4.0）:
  用户照片 → rembg去背景 → 摄影棚背景合成 → Wan2.7姿态优化 → 干净模特图
                                                            ↓
                                          aitryon-plus 试衣 → 高质量效果图
```

## 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `tools/ai_proxy_server.py` | 修改 | v0.4.0，新增 generate-model + preprocess_body |
| `lib/core/network/api_config.dart` | 修改 | 新增 generateModelPath |
| `lib/core/network/api_client.dart` | 修改 | 新增 generateModel()；tryOnBytes 超时修复 + preprocessBody |
| `lib/core/network/try_on_service.dart` | 修改 | 新增 generateModel()；tryOnSingle 支持 preprocessBody |
| `lib/features/fitting_room/fitting_room_page.dart` | 修改 | 步骤1 AI 模特化 + 步骤4 自动预处理 |
| `tools/test_generate_model.py` | 新建 | generate-model 端点测试脚本 |
| `tools/test_tryon_pipeline.py` | 新建 | try-on 流水线测试脚本 |

## 成本估算

| 步骤 | API | 单价 | 耗时 |
|------|-----|------|------|
| 去背景 | rembg（本地） | 免费 | ~3s |
| 姿态优化 | Wan2.7 img2img | ~¥0.04/张 | ~20s |
| 虚拟试衣 | aitryon-plus | ¥0.50/张 | ~90s |
| **合计** | | **~¥0.54/次** | **~115s** |

## 当前状态总结

| 组件 | 状态 | 说明 |
|------|------|------|
| 代理服务器 | ✅ v0.4.0 | 4个端点全功能运行 |
| AI 模特化 | ✅ 通过 | rembg+Wan2.7 流水线验证通过 |
| AI 试衣 | ✅ 通过 | 支持 preprocess_body 一步到位 |
| Flutter 试衣间 | ✅ 接入完成 | 步骤1自动处理 + 步骤4自动兜底 |
| 示例衣物数据 | ⚠️ | 仍是无 imageBytes 的占位数据 |

## 待办事项

- [ ] 用真实人物照片测试模特化效果（非生成的测试图）
- [ ] 补充示例衣物的真实 imageBytes（或接入衣柜页流程）
- [ ] 可考虑调优 Wan2.7 prompt 以获得更好的模特效果

---

## 第二次会话 — 试衣间 Step 1 重构（Photo + Measurements 双模式）

### 完成

- [x] **BodyProfile 模型扩展**：新增 `Gender` 枚举（male/female），新增 `gender` 字段和 `genderLabel` getter
- [x] **ManualMeasureForm 性别选择器**：新增 pill 按钮（男/女），默认选中"女"，UI 沿用 segmented-tab 风格
- [x] **BodySetupSheet 拍照模式重构**：选图后显示性别/身高/体重表单，确认按钮改为"AI 生成模特图"
- [x] **FittingRoomPage 双模式生成**：
  - 拍照模式：传 gender/height/weight 到 `generateModel()`
  - 手动模式：新增 `_generateModelFromMeasurements()` → Wan2.7 文生图
  - `_ModelGenerationDialog` 区分两种模式的文案
- [x] **ApiClient.generateModel() 扩展**：imageBytes 改为可选，新增 gender/heightCm/weightKg/bustCm/waistCm/hipCm 表单字段
- [x] **TryOnService 扩展**：`generateModel()` 新增测量参数；新增 `generateModelFromMeasurements()` 手动模式方法
- [x] **代理服务器 v0.5.0**：
  - `/api/generate-model` 双模式：有图→img2img（紧身衣prompt），无图→t2i（从测量数据生成人物）
  - 新增 `build_model_prompt()` 提示词工厂函数
  - 新增 `call_wan27_t2i()` 文生图函数
  - `call_wan27_img2img()` 支持自定义 prompt
  - `preprocess_body_image()` 接受测量参数

### 架构变更

```
新流程 v0.5.0:

📷 拍照模式:
  照片 + 性别 + 身高 + 体重 → rembg去背景 → 合成#F0F0F3
    → Wan2.7 img2img（紧身衣 + 标准站姿 + 干净背景）
    → 模特图 JPEG

📐 手动模式:
  性别 + 身高 + 体重 + 三围 → Wan2.7 t2i（文生图）
    → 人物形象 JPEG（紧身运动服 + 摄影棚背景）
```

### 验证

- `flutter analyze` — 零 error 零 warning ✅
- Python 语法检查通过 ✅
- Flutter Web 启动成功（localhost:4000）

### 待办

- [ ] 启动代理服务器 → 实测拍照模式完整流程
- [ ] 实测手动模式文生图效果
- [ ] 验证 proxy server 是否正确接收新增 form 字段
