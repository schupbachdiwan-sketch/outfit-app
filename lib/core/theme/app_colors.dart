import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 主色调 — 方案A：柔和紫罗兰
  static const primary = Color(0xFF6C5CE7);
  static const primaryLight = Color(0xFFF0EEFF);
  static const accent = Color(0xFFFF6B6B);

  // 背景 & 表面
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFEEEEF0);

  // 文字色
  static const textPrimary = Color(0xFF1E1E2E);
  static const textSecondary = Color(0xFF9E9EAA);
  static const textPlaceholder = Color(0xFFBEBEC4);
  static const textOnDark = Color(0xFFFFFFFF);

  // 功能色
  static const success = Color(0xFF4CD964);
  static const warning = Color(0xFFFF9500);
  static const error = Color(0xFFFF3B30);
}
