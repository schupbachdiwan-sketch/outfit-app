import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 主色调
  static const primary = Color(0xFF7C83FD);
  static const primaryLight = Color(0xFFE8E9FF);
  static const accent = Color(0xFFFF9191);

  // 背景 & 表面
  static const background = Color(0xFFFAFAFC);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFEEEEF0);

  // 文字色
  static const textPrimary = Color(0xFF2D2D3A);
  static const textSecondary = Color(0xFF8E8E9A);
  static const textPlaceholder = Color(0xFFC4C4CC);
  static const textOnDark = Color(0xFFFFFFFF);

  // 功能色
  static const success = Color(0xFF4CD964);
  static const warning = Color(0xFFFF9500);
  static const error = Color(0xFFFF3B30);
}
