[phases.setup]
nixPkgs = ["python311", "gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install -r tools/requirements.txt"]

[start]
cmd = "python tools/ai_proxy_server.py --host 0.0.0.0 --port $PORT"